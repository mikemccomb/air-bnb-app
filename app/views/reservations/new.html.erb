<h1>Reserve this room</h1>

<%= form_with method: "post", url: "/reservations" do |form| %>
  <div>
    <p><%= form.label :start_date, "Check-in Date:" %>
    <%= form.date_field :start_date %></p>
    <p><%= form.label :end_date, "Check-out Date:" %>
    <%= form.date_field :end_date %></p>
    <p><%= form.label :room_id, "Room ID:" %>
    <%= params[:room_id] %>
    </p>
    <p><%= form.label :price, "Price per night:" %>
    <%= params[:price] %>
    </p>
    <p><strong>Total Cost:</strong> <%= reservation.total_cost%></p>
  </div>
  <div>
    <%= form.submit "Complete Reservation" %>
  </div>

<% end%> 

<!-- 
  # Fix room_id
  # Fix Price
  # Calculate total
  # Where to go after clicking submit
  # Link to leave review
  >